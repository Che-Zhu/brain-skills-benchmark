{
  "skill_name": "sealos-deploy",
  "source": "../sealos-skills/skills/sealos-deploy/evals/evals.json",
  "synced_at": "2026-06-02T03:53:04.744Z",
  "cases": [
    {
      "id": 0,
      "name": "louislam/uptime-kuma@master",
      "prompt": "/sealos-deploy louislam/uptime-kuma master with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "requires-repository-branch-token",
        "pushes-ghcr-image",
        "writes-brain-templates",
        "response-json-only",
        "success-image-tagged",
        "does-not-leak-token"
      ],
      "notes": "Checks out the requested repository branch, prepares a Dockerfile/build context, pushes a tagged GHCR image, writes a fixed GitHubDeployResponse with Brain Crossplane templates, and returns only the fixed GitHubDeployResponse JSON."
    },
    {
      "id": 1,
      "name": "sharkdp/bat@master",
      "prompt": "/sealos-deploy sharkdp/bat master with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-fail",
      "assertionNames": [
        "identifies-cli-tool",
        "stops-before-ghcr-push",
        "response-json-only",
        "actionable-error"
      ],
      "notes": "Identifies the repository as unsuitable for deployment before build, then returns the fixed failed GitHubDeployResponse JSON shape."
    },
    {
      "id": 2,
      "name": "with@no",
      "prompt": "/sealos-deploy with no Repository, branch, or GITHUB_TOKEN",
      "expectation": "should-fail",
      "assertionNames": [
        "requires-repository",
        "requires-branch",
        "requires-github-token",
        "response-json-only"
      ],
      "notes": "Fails preflight and returns the fixed failed GitHubDeployResponse JSON shape explaining the missing required inputs."
    },
    {
      "id": 3,
      "name": "alist-org/alist@main",
      "prompt": "/sealos-deploy alist-org/alist main with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "identifies-go",
        "resolves-branch-to-sha",
        "build-request-required",
        "brain-ap-image-port",
        "response-json-only"
      ],
      "notes": "Builds the requested Go web application branch, pushes a tagged GHCR image, writes a fixed GitHubDeployResponse with Brain Crossplane templates, and returns the fixed succeeded GitHubDeployResponse JSON shape."
    },
    {
      "id": 4,
      "name": "lobehub/lobe-chat@main",
      "prompt": "/sealos-deploy lobehub/lobe-chat main with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "identifies-nextjs",
        "detects-monorepo-or-workspace",
        "uses-sandbox-context",
        "uses-brain-default-composition",
        "response-json-only"
      ],
      "notes": "Handles a Next.js monorepo build context, pushes a tagged GHCR image, writes a fixed GitHubDeployResponse with Brain Crossplane templates, and returns the fixed succeeded GitHubDeployResponse JSON shape."
    },
    {
      "id": 5,
      "name": "jgraph/drawio-desktop@dev",
      "prompt": "/sealos-deploy jgraph/drawio-desktop dev with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-fail",
      "assertionNames": [
        "identifies-desktop-app",
        "stops-before-ghcr-push",
        "response-json-only"
      ],
      "notes": "Identifies the repository as an Electron desktop app unsuitable for cloud deployment and returns the fixed failed GitHubDeployResponse JSON shape."
    },
    {
      "id": 6,
      "name": "dani-garcia/vaultwarden@main",
      "prompt": "/sealos-deploy dani-garcia/vaultwarden main with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "identifies-rust",
        "identifies-web-service",
        "success-image-tagged",
        "does-not-leak-token"
      ],
      "notes": "Builds the requested Rust web application branch, pushes a tagged GHCR image, writes a fixed GitHubDeployResponse with Brain Crossplane templates, and returns the fixed succeeded GitHubDeployResponse JSON shape."
    },
    {
      "id": 7,
      "name": "pallets/flask@main",
      "prompt": "/sealos-deploy pallets/flask main with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-fail",
      "assertionNames": [
        "identifies-library",
        "stops-before-ghcr-push",
        "response-json-only",
        "actionable-error"
      ],
      "notes": "Identifies Flask as a framework/library rather than a standalone deployable app and returns the fixed failed GitHubDeployResponse JSON shape."
    },
    {
      "id": 8,
      "name": "mealie-recipes/mealie@mealie-next",
      "prompt": "/sealos-deploy mealie-recipes/mealie mealie-next with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "identifies-python",
        "detects-external-db",
        "generates-db-template",
        "build-request-required",
        "required-env-guard",
        "response-json-only"
      ],
      "notes": "Builds the requested Python FastAPI application branch, pushes a tagged GHCR image, writes a fixed GitHubDeployResponse with Brain Crossplane templates, and returns the fixed succeeded GitHubDeployResponse JSON shape."
    },
    {
      "id": 9,
      "name": "spring-projects/spring-petclinic@main",
      "prompt": "/sealos-deploy spring-projects/spring-petclinic main with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "identifies-java",
        "identifies-web-service",
        "resolves-branch-to-sha",
        "response-json-only"
      ],
      "notes": "Builds the requested Java Spring Boot application branch, pushes a tagged GHCR image, writes a fixed GitHubDeployResponse with Brain Crossplane templates, and returns the fixed succeeded GitHubDeployResponse JSON shape."
    },
    {
      "id": 10,
      "name": "an@app",
      "prompt": "/sealos-deploy an app repository that uses @aws-sdk/client-s3 and PostgreSQL with GITHUB_TOKEN provided by the sandbox",
      "expectation": "should-succeed",
      "assertionNames": [
        "detects-object-storage",
        "generates-object-storage-template",
        "generates-db-template",
        "orders-dependencies-before-app",
        "response-json-only"
      ],
      "notes": "Builds the project image, detects both database and S3-compatible object storage dependencies, and returns a GitHubDeployResponse containing ap:web plus dependency templates that can be applied in order."
    }
  ]
}
